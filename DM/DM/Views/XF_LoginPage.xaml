<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="DM.Views.XF_LoginPage">
    <ContentPage.Content>
        <StackLayout>
            <Entry x:Name = "Steam_ID" Placeholder = "Steam ID" Text="{Binding Steam_64_id}"
                    />

            <Button x:Name= "LoginBtn" Text = "Login " Command="{Binding LoginCommand}"
                    />

            <Button x:Name= "Login" Text = "Login Steam" Command="{Binding LoginSteamCommand}"
                    />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>